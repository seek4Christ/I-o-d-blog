<h1> <%= Comment.human_attribute_name(:Articles Tagged with) %>
  <%= @tag.name %></h1>

<ul>
  <% @tag.articles.each do |article| %>
    <li><%= t('link_to article.title, article_path(article')) %></li>
  <% end %>
</ul>
